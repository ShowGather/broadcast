# Local development only. Copy this file to .env and choose a non-production password.
# Keep the password in DATABASE_URL in sync with POSTGRES_PASSWORD.
POSTGRES_PASSWORD=replace-with-a-local-development-password
POSTGRES_PORT=5433
DATABASE_URL=postgresql://showgather:replace-with-a-local-development-password@localhost:5433/showgather?schema=public
